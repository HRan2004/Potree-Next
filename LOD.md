# LOD (Level of Detail) 细节层次系统详解

## 一、LOD 基础概念

### 1.1 什么是 LOD

LOD (Level of Detail，细节层次) 是计算机图形学中的一项核心优化技术。其基本思想是：根据物体与观察者的距离或重要性，动态选择不同精度的模型进行渲染。

**核心原理**：

- **近距离物体**：使用高精度模型，展示丰富细节
- **远距离物体**：使用低精度模型，减少渲染开销
- **动态切换**：随着相机移动，实时调整模型精度

**为什么需要 LOD**：

- 大规模场景中，同时渲染所有细节会导致性能瓶颈
- 远处物体在屏幕上只占少量像素，高精度模型的细节无法被察觉
- 通过 LOD 可以在保证视觉质量的前提下，大幅提升渲染性能

### 1.2 点云场景中的 LOD 挑战

点云数据具有特殊性：

- **数据量巨大**：一个场景可能包含数十亿个点
- **无结构化**：点是离散的，不像网格有明确的拓扑关系
- **实时性要求**：需要在每一帧快速决定显示哪些点

传统的网格 LOD 方案（如简化多边形数量）不适用于点云。点云需要专门的空间组织结构来支持 LOD。

## 二、八叉树：点云 LOD 的空间基础

### 2.1 八叉树结构

八叉树 (Octree) 是一种三维空间递归细分结构。它将三维空间不断等分为 8 个子立方体，形成树状层级：

```
Level 0 (根节点)     Level 1              Level 2

    1 个节点           8 个节点           64 个节点
    覆盖整个空间       每个覆盖 1/8       每个覆盖 1/64
```

**层级与精度的关系**：

- **Level 0（根节点）**：包含整个场景的粗略采样，点数最少
- **Level 1**：8 个子节点，每个包含对应区域的更密集采样
- **Level N**：越深层级，空间划分越细，点的密度越高，细节越丰富

八叉树天然形成了一个从粗到细的多分辨率结构，完美契合 LOD 的需求。

### 2.2 八叉树节点的数据组织

每个八叉树节点存储：

- **空间范围**：该节点覆盖的三维包围盒
- **点数据**：属于该空间范围内、该精度层级的点
- **子节点引用**：最多 8 个子节点的指针
- **加载状态**：数据是否已从服务器加载到内存

节点的点数据并非包含其空间范围内的所有点，而是该层级对应精度的采样子集。所有层级的点合在一起才构成完整的点云。

## 三、可见性更新算法

可见性更新是 LOD 系统的核心，每一帧都会执行。它决定了当前帧应该渲染八叉树中的哪些节点。

### 3.1 整体流程

算法采用优先级队列驱动的广度优先遍历策略：

1. **初始化**：将八叉树根节点放入优先级队列
2. **循环弹出**：每次从队列中取出权重最高（最重要）的节点
3. **加载检查**：如果节点数据未加载，将其加入加载队列，跳过
4. **视锥体剔除**：判断节点是否在相机可见范围内，不可见则跳过
5. **标记可见**：将通过检查的节点标记为本帧可见
6. **子节点展开**：计算子节点的屏幕空间大小，若足够大则加入优先级队列
7. **预算控制**：累计点数超过点预算时停止展开

这个过程从根节点开始，逐层向下展开，直到满足精度要求或耗尽点预算。

### 3.2 优先级队列

优先级队列是算法的调度核心。它是一个基于二叉堆的数据结构，按节点的"权重"排序，权重越高的节点越先被处理。

权重的含义是：该节点在当前视角下的重要程度。权重高意味着这个节点在屏幕上占据较大面积，或者处于用户关注的区域，应该优先加载和渲染。

### 3.3 权重计算

节点权重由两个因素共同决定：**屏幕空间像素大小** 和 **屏幕中心加成**。

**屏幕空间像素大小**

这是权重的主要分量。它衡量的是：一个节点的包围球投影到屏幕上后，占据多少像素。

计算过程：

1. 计算相机到节点中心的距离 `distance`
2. 根据相机视场角 (FOV) 计算投影因子：`projFactor = 1 / (tan(fov/2) * distance)`
3. 最终像素大小：`pixelSize = nodeRadius * projFactor * framebufferHeight`

直觉理解：

- 节点离相机越近，`distance` 越小，`projFactor` 越大，像素大小越大
- 节点本身空间范围越大（`nodeRadius` 越大），像素大小也越大
- 屏幕分辨率越高（`framebufferHeight` 越大），像素大小越大

当像素大小低于一个阈值（`minNodeSize`）时，说明该节点在屏幕上太小了，不值得继续细分，子节点不会被加入队列。

**屏幕中心加成**

在像素大小的基础上，还会根据节点在屏幕上的位置给予额外加成：

1. 将节点中心投影到屏幕的归一化设备坐标 (NDC)，得到 `screenU` 和 `screenV`（范围 [-1, 1]）
2. 计算到屏幕中心的距离：`distanceToCenter = sqrt(screenU² + screenV²)`
3. 中心权重：`centerWeight = clamp(1 - distanceToCenter, 0, 1) + 0.5`

这意味着屏幕中心的节点会获得最高 1.5 倍的权重加成，而屏幕边缘的节点只有 0.5 倍。这符合人眼的注意力分布——用户通常关注屏幕中央区域。

**最终权重**：`weight = pixelSize * centerWeight`

### 3.4 视锥体剔除

视锥体剔除是一项重要的优化手段。相机的视锥体定义了可见空间的范围，超出这个范围的节点不会被用户看到，因此无需渲染。

**剔除判断**：

- 计算节点的包围球（中心点 + 半径）
- 判断包围球是否与视锥体相交
- 如果不相交且节点层级大于 2，则跳过该节点及其子树

**为什么层级 ≤ 2 的节点不剔除**：

- 根节点和第一、二层节点覆盖范围很大，即使中心不在视锥体内，也可能有部分区域可见
- 过早剔除可能导致大片区域消失
- 深层节点空间范围小，剔除判断更精确

### 3.5 点预算控制

点预算 (Point Budget) 是 LOD 系统的性能保障机制。它限制了每一帧最多渲染多少个点，确保帧率稳定。

**工作原理**：

1. 在可见性更新过程中，累计所有可见节点的点数
2. 当累计点数超过预算阈值（默认 200 万点）时，停止展开新的子节点
3. 已经在队列中的节点仍会被处理，但不再向队列添加新节点

**效果**：

- 近处区域：由于权重高，优先被展开到深层级，获得高精度
- 远处区域：权重低，可能只展开到浅层级，使用低精度
- 整体点数被控制在预算范围内，保证性能

这种策略实现了"重点突出"的渲染效果：用户关注的区域细节丰富，外围区域适度简化。

## 四、细化模式

细化模式 (Refinement Mode) 决定了当子节点加载完成后，父节点和子节点之间如何协作显示。系统支持两种模式。

### 4.1 ADDITIVE 模式（累加模式）

**行为**：父节点和子节点同时显示，所有层级的点叠加在一起。

**工作方式**：

- 根节点的粗略采样始终显示
- 加载 Level 1 后，Level 0 + Level 1 的点同时渲染
- 加载 Level 2 后，Level 0 + Level 1 + Level 2 的点同时渲染
- 以此类推，层级越深，点越密集

**适用场景**：

- Potree v2 格式的默认模式
- 适合纯点数据的渲染
- 远近都能显示完整细节

**缺点**：

- 存在重复渲染，父节点的点和子节点的点可能在空间上重叠
- 渲染的总点数较多，性能开销较大

### 4.2 REPLACING 模式（替换模式）

**行为**：当子节点全部加载完成后，隐藏父节点，只显示子节点。

**工作方式**：

- 初始只显示根节点
- 当根节点的所有子节点都加载完成后，隐藏根节点，显示子节点
- 对每个可见节点递归执行相同逻辑
- 如果某个节点的子节点只加载了一部分，则父节点继续显示（避免出现空洞）

**替换条件**：

- 至少有一个子节点可见（说明需要更高精度）
- 所有子节点都已加载完成（确保替换后没有空洞）
- 两个条件同时满足时，才执行替换

**适用场景**：

- Potree v3 格式的默认模式
- 特别适合体素数据的渲染
- 避免了父子节点的重复渲染，性能更优

### 4.3 两种模式的对比

```
ADDITIVE 模式（累加）：

Level 0:  ○ ○ ○ ○           ← 始终显示
Level 1:  ● ● ● ● ● ● ● ●  ← 叠加显示
          所有层级的点共同构成画面

REPLACING 模式（替换）：

加载前:   ○ ○ ○ ○           ← 只显示父节点
加载后:   ● ● ● ● ● ● ● ●  ← 子节点替换父节点
          同一时刻只有一个层级的节点可见
```

## 五、异步加载机制

LOD 系统的一个关键特性是按需加载：只有当节点需要显示时，才从服务器加载其数据。这避免了一次性加载整个点云造成的巨大开销。

### 5.1 加载队列

可见性更新过程中，遇到未加载的节点时，会将其加入加载队列。加载队列有两个限制：

**队列容量限制**：最多 40 个节点等待加载

- 防止队列无限增长
- 超过限制时，新节点不会被加入，等待下一帧再尝试

**并发加载限制**：最多 10 个节点同时加载

- 控制网络请求的并发数
- 避免同时发起过多 HTTP 请求导致浏览器或服务器过载

### 5.2 加载优先级

加载队列中的节点按照它们在优先级队列中的顺序处理。这意味着：

- 权重高的节点（屏幕上大、靠近中心）优先加载
- 权重低的节点（屏幕上小、靠近边缘）延后加载

这确保了用户最关注的区域能够最快获得高精度数据。

### 5.3 加载流程

节点加载分为两个阶段：

**阶段 1：层级结构加载**（仅针对 PROXY 节点）

- PROXY 节点是占位符，只包含空间范围信息，不包含子节点列表
- 首次访问时，从服务器的层级文件中读取该节点的子节点信息
- 解析后创建 8 个子节点对象，建立父子关系
- 子节点初始也是 PROXY 状态

**阶段 2：点数据加载**

- 通过 HTTP Range 请求，只下载该节点对应的数据块
- 数据发送到 Worker 线程进行解码和坐标变换
- 解码完成后，数据上传到 GPU 缓冲区
- 节点标记为已加载，下一帧可以参与渲染

## 六、LRU 内存管理

随着用户在场景中漫游，不断有新节点被加载。如果不释放不再需要的节点，GPU 内存会持续增长直到耗尽。LRU (Least Recently Used，最近最少使用) 缓存机制解决了这个问题。

### 6.1 工作原理

每个已加载的节点在 LRU 链表中维护一个条目，记录其最后一次被访问（渲染）的时间戳。

**每帧更新**：

- 当节点被标记为可见时，更新其时间戳，将其移到链表头部
- 链表尾部的节点就是最久未被访问的节点

### 6.2 清理策略

每帧结束时执行清理检查，触发条件：

- 缓存中的节点数量 ≥ 10
- 最新节点和最老节点的时间戳差距 > 100ms

满足条件时，从链表尾部取出最老的节点，执行清理：

- 释放该节点的 GPU 缓冲区
- 清空几何数据
- 将节点状态重置为未加载
- 从 LRU 链表中移除

**递归清理**：被清理的节点的所有子孙节点也会一并清理。因为如果父节点不可见，子节点也不会被访问到。

### 6.3 设计意义

LRU 机制使得系统能够在有限的内存中处理无限大的场景：

- 用户向前移动时，前方的节点被加载，后方的节点被释放
- 用户回头看时，之前释放的节点会重新加载
- 内存使用量保持在一个相对稳定的水平

## 七、体素渲染与 LOD

### 7.1 体素的概念

体素 (Voxel) = Volume + Pixel，即三维空间中的立方体单元。可以理解为三维的像素。

在 Potree v3 中，八叉树的内节点（非叶子节点）使用体素而非点来表示数据。每个体素是一个小立方体，代表其所在空间区域的平均颜色。

### 7.2 体素与 LOD 的配合

体素在 LOD 系统中扮演了重要角色：

**远距离渲染**：

- 远处的节点使用体素渲染
- 少量体素即可覆盖大面积区域，渲染效率高
- 立方体之间无缝隙，视觉上连续平滑

**近距离渲染**：

- 近处的节点切换到点数据渲染
- 保留原始点云的精细细节

**过渡**：

- 配合 REPLACING 细化模式，远处显示体素，近处显示点
- 随着相机靠近，体素节点被更精细的子节点替换
- 实现从粗糙到精细的平滑过渡

### 7.3 Splat 类型

系统支持三种渲染模式，对应不同的 LOD 层级需求：

| 类型          | 每个元素的顶点数 | 几何形状       | 用途                 |
| ------------- | ---------------- | -------------- | -------------------- |
| 点 (Point)    | 1                | GPU 原生点精灵 | 标准点云渲染         |
| 四边形 (Quad) | 6                | 面向相机的矩形 | 自定义大小的点       |
| 体素 (Voxel)  | 36               | 立方体         | 内节点的空间填充渲染 |

体素虽然每个元素需要 36 个顶点（立方体的 12 个三角形），但由于远距离时体素数量远少于点数量，总体渲染开销反而更低。

## 八、完整的一帧 LOD 流程总结

将以上所有环节串联起来，一帧中 LOD 系统的完整工作流程如下：

```
1. 开始新的一帧
   │
2. 初始化优先级队列，放入根节点
   │
3. 从队列中取出权重最高的节点
   │
4. ├─ 未加载？→ 加入加载队列 → 回到步骤 3
   │
5. ├─ 不在视锥体内？→ 跳过 → 回到步骤 3
   │
6. ├─ 标记为可见，更新 LRU 时间戳
   │
7. ├─ 累计点数超过预算？→ 停止展开
   │  │
   │  └─ 未超预算 → 计算子节点权重
   │     │
   │     └─ 子节点像素大小 ≥ 阈值？→ 加入优先级队列
   │
8. 队列为空或预算耗尽 → 结束遍历
   │
9. 应用细化模式（ADDITIVE 或 REPLACING）
   │
10. 执行 LRU 清理，释放过期节点
   │
11. 将可见节点提交给渲染管线绘制
```

这个流程每帧执行一次，确保在任何视角下都能以最优的精度分配渲染点云数据。
